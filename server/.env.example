DATABASE_URL="postgresql://ghostline:changeme@localhost:5432/ghostline"
JWT_SECRET="change-this-to-a-long-random-string"
PORT=4000
CORS_ORIGIN="*"
UPLOADS_DIR="./uploads"

# Telegram admin notifications (optional)
# 1. Create a bot via @BotFather, paste its token here
# 2. Message the bot, then visit https://api.telegram.org/bot<token>/getUpdates
#    to find your chat id. Multiple admins: comma-separate the ids.
TELEGRAM_BOT_TOKEN=""
TELEGRAM_ADMIN_CHAT_IDS=""
