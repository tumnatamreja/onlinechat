// GhostLine standalone chat page configuration.
// Edit these values to point at your own server — no rebuild needed,
// this file is loaded as a plain <script> tag.
window.GHOSTLINE_CONFIG = {
  // URL of your GhostLine server (the one running server/src/index.ts)
  serverUrl: 'http://localhost:4000',

  // Optional label shown to operators (e.g. which page the chat was opened from)
  label: 'Website Visitor',
};
